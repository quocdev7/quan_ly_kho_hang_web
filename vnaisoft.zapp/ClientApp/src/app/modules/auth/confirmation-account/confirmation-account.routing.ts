import { Route } from '@angular/router';
import { ConfirmationAccountComponent } from './confirmation-account.component';

export const confirmationaccountRoutes: Route[] = [
    {
        path     : '',
        component: ConfirmationAccountComponent
    }
];
